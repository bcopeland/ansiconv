// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("ansiconv", "for ansiconv support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("ansiconv", "enable ansiconv support", "no");

if (PHP_ANSICONV != "no") {
	EXTENSION("ansiconv", "ansiconv.c");
}

